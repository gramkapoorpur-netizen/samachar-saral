services:
  api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  worker:
    build: .
    command: celery -A app.tasks.ingest worker --beat --loglevel=info
    env_file:
      - .env
    depends_on:
      - redis
